spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bprueba
    username: postgres
    password: riorimac123
    driver-class-name: org.postgresql.Driver
  devtools:
    restart:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  port: 8080
